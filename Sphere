using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sphere : MonoBehaviour
{
    
    public bool hhhhh = false;

    // Start is called before the first frame update
    void Start()
    {

        hhhhh = true;

     

    }

    // Update is called once per frame
    void Update()
    {
     Debug.Log(hhhhh);
    }
}
